const fs = require('fs');

const data = new Float32Array([0, 0, 150, 0, 0, 150]);
// const data = new Float64Array([0.5, 0, 0, 0.5, -0.5, 0]);

fs.writeFile('data.data', data, { mode: null }, console.log);